/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
# local secrets — never committed
.env
# the bots' persisted conversation log
*.jsonl

# Rust sandbox build artifacts (the built .wasm is committed under priv/wasm/)
/native/js_sandbox/target/
